using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemManager : MonoBehaviour {

	public enum Mode { Arena }

//	public static Item Random()
//	{
//		var items = System.Enum.GetValues(typeof(Item.Types));
//		var randomItemIndex = new System.Random().Next(0, items.Length - 1);
//		return new Item((Item.Types)items.GetValue(randomItemIndex));
//	}
}
